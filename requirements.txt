# Core Crawl4AI library
crawl4ai==0.4.247  # Replace with the latest or desired version

# Optional: Advanced features (uncomment if needed)
# transformers>=4.0.0
# torch>=1.7.0

# Optional: Playwright (if not handled by crawl4ai-setup)
# playwright>=1.20.0
